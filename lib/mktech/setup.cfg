[flake8]
ignore =
    # Allow under-indented continuation for compactness.
    E128,
    # Ignore F403, F405 so build scripts can use * imports.
    F403, F405,
    # We'll line break before binary operators.
    W503
exclude =
    # Caches, etc.
    .*,
    # pip build directory
    ./build
